{
  "name": "ai-agent-platform",
  "version": "1.0.0",
  "description": "AI Agent Platform on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute ai_agent_database --file=./migrations/001_initial.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "itty-router": "^4.0.24"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
